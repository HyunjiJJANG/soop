<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="kr.co.jhta.soop.dao.memberProjectProjectmemberDAO">
	
	<select id="selectAll" resultType="kr.co.jhta.soop.dto.memberProjectProjectmemberDTO">
		SELECT m.member_name
		FROM member m
		JOIN project_member pm
		ON m.member_no = pm.member_no
		JOIN project p
		ON pm.project_no = p.project_no
		WHERE m.member_no = #{member_no}
	</select>
	
</mapper>