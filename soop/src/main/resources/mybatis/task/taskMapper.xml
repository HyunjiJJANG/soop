<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="kr.co.jhta.soop.dao.TaskDAO">
	
	<select id="selectAll" resultType="TaskDTO" >
		SELECT task_no, project_no, member_no, task_title, task_register_date, task_content, task_status, task_start_date, task_end_date
		FROM task
	</select>

	<!-- <select id="selectOne" resultType="TaskDTO" parameterType="int">
		SELECT task_no, project_no, member_no, task_title, task_register_date, task_content, task_status, task_start_date, task_end_date
		FROM task
		WHERE task_no = #{task_no}
	</select>
	
	<update id="updateOne" parameterType="TaskDTO">
		UPDATE task
		SET task_title = #{task_title}, task_register_date = sysdate, task_content = #{task_content}, task_status = #{task_status}, task_start_date = #{task_start_date}, task_end_date = #{task_end_date} 
		WHERE task_no = #{task_no}
	</update>

	<insert id="insertOne" parameterType="TaskDTO">
		INSERT INTO task(project_no, member_no, task_title, task_register_date, task_content, task_status, task_start_date, task_end_date)
		VALUES (#{project_no}, #{member_no}, #{task_title}, sysdate, #{task_content}, #{task_status}, #{task_start_date}, #{task_end_date})
	</insert>
	
	<delete id="deleteOne" parameterType="int">
		DELETE task
		WHERE task_no = #{task_no}
	</delete> -->
	
</mapper>